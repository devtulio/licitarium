# Ferramentas de desenvolvimento — não entram no exe.
# O runtime está em requirements.txt; instale os dois para trabalhar no
# projeto:  pip install -r requirements.txt -r requirements-dev.txt
#
# O E2E (Playwright) é Node e vem do package.json:  npm install
pytest>=8
pytest-cov>=5      # cobertura: python -m pytest tests/ --cov=. --cov-report=term-missing
pyinstaller>=6     # empacota o exe; o CI usa a mesma ferramenta
